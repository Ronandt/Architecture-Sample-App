# =============================================================================
# Frontend subchart metadata
# Manages the React/Vite application served by nginx-unprivileged.
# =============================================================================
apiVersion: v2
name: frontend
description: React 19 + Vite frontend served by nginx-unprivileged (OpenShift-compatible)
type: application
version: 0.1.0
appVersion: "1.0.0"
